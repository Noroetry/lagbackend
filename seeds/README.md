# Seeds

Coloca aquí los archivos JSON de datos iniciales para poblar la base de datos automáticamente.

- `objects.json`: objetos del sistema (experiencia, moneda, etc.)
- `quests.json`: misiones iniciales

Puedes editar estos archivos y reiniciar el servidor para que los datos se aseguren en la base de datos si no existen.
